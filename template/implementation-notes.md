# Implementation notes: [short title]

Plan: `plans/[same-date-slug].md`
Date started: [YYYY-MM-DD]

Log entries as work happens, not retroactively summarized at the end. One entry per notable
event: a completed item, a deviation from the plan, a bug found mid-implementation, a decision
made that the plan didn't anticipate.

## [YYYY-MM-DD] — [branch/item being worked]

- What was implemented, with file:line references.
- **Deviation from plan** (if any): what the plan said vs. what was actually done, and why. Only
  write this section when there is an actual deviation — no entry needed for "implemented exactly
  as planned."
- Anything discovered during implementation that the plan didn't anticipate (a hidden constraint,
  an existing bug, a wrong assumption in the plan) — note it here even if it didn't change the
  outcome, so a future reader knows it was considered.

## Open items carried forward

[Anything deferred, TODO, or flagged for a later branch/session — so nothing gets silently
dropped between sessions.]
